<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.won_tonsoup.uniformmarker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:noHistory="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListActivity" />
        <activity
            android:name=".DetailActivity"
            android:label="@string/CadetLast" />
        <activity android:name=".EditListActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".AddNewCadetActivity" />
        <activity android:name=".DataMenu" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".IndividualData" />
        <activity android:name=".IndividualDataList" />
        <activity android:name=".HelpActivity"></activity>
    </application>

</manifest>